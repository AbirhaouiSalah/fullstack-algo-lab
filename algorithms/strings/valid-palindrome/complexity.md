# Complexity Analysis: Valid Palindrome

## Time Complexity

`O(n)` â€” each character is visited at most once across the two pointers.

## Space Complexity

`O(1)` â€” no auxiliary string or array is built; comparisons happen directly
on character codes.

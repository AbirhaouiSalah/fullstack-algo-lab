# Complexity Analysis: Rotate Array Inplace

## Time Complexity

`O(n)` â€” each element is touched a constant number of times across the three
reversal passes.

## Space Complexity

`O(1)` â€” rotation happens in place; no auxiliary array is allocated.

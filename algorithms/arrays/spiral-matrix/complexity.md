# Complexity Analysis: Spiral Matrix

## Time Complexity

`O(m * n)` â€” every cell is visited exactly once.

## Space Complexity

`O(1)` extra space, excluding the output array (which necessarily holds
`m * n` elements).

# Big-O Complexity Guide

## Common Time Complexities (best to worst)

- O(1) â€” Constant
- O(log n) â€” Logarithmic
- O(n) â€” Linear
- O(n log n) â€” Linearithmic
- O(n^2) â€” Quadratic
- O(2^n) â€” Exponential
- O(n!) â€” Factorial

## Analysis Checklist

- Count loop iterations relative to input size.
- Account for nested loops multiplicatively.
- Recursive calls: use the recurrence relation / Master Theorem.
- Don't forget space used by recursion stack, auxiliary structures.
